export declare enum StatusReasons {
    created = "created",
    updated = "updated",
    deleted = "deleted",
    modified = "modified",
    unknown = "unknown",
    ready = "ready"
}
export declare enum DownloadTypes {
    HUGGINGFACE_DL = "huggingface-dl",
    WGET = "wget"
}
